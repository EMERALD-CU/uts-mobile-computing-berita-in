name: berita_in
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2             # Untuk State Management
  shared_preferences: ^2.2.3   # Untuk Local Storage (Status Login)
  http: ^1.2.1                 # Untuk Integrasi REST API News

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # Konfigurasi Custom Font Gagalin
  fonts:
    - family: Gagalin
      fonts:
        - asset: lib/assets/fonts/Gagalin-Regular.otf